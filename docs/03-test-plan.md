# Test plan

Machine-generated by `cargo run -p xtask -- traceability --write` (NFR-QUAL-010, FRS §10). Do not hand-edit -- regenerate instead. Maps every Must-priority requirement to how it is verified: a manual-test document (`Verify: M`) or the crate(s) whose test source carries a `trace:` annotation or matching test-function name for it (`Verify: U/I/G/B/S`). A `Verify: M` row resolves only when its document's own `**Result:` line records a clean pass -- a document recording `NOT EXECUTED`, a partial or a qualified pass, and a document carrying no verdict line at all, leave the requirement UNRESOLVED with the document still named (issue #34). A row marked "PARTIAL" carries a `// trace-partial:` tag whose artifact covers only part of the requirement: the text after it is that tag's mandatory `// uncovered:` line verbatim, naming the unspanned member and the milestone that closes it (D-23.1). A partial counts as coverage for this check's exit status and is not Done for §14's table. A requirement listed under "UNRESOLVED" has neither -- `cargo run -p xtask -- traceability` exits non-zero while any remain. CI's **required** step passes `--allow-uncovered` and gates on this file's freshness (and on §14's denominators) alone; the zero-uncovered half stays informational until it becomes required at M9b's close-out (D-18.5).

| Requirement | Verify | Covered by |
|---|---|---|
| FR-CFG-010 | S | `workspace` |
| FR-CFG-020 | G | `namir-clap` |
| FR-CFG-030 | I | **PARTIAL** — `xtask`: FR-CFG-030 — the Verify: I method's "each is installed alone into a clean environment and exercised" is executed by nothing: the artifact is xtask layering's compile-time dependency-edge lint over LAYERING_TABLE, which argues compile-time reachability and neither installs nor exercises either product; closes M8 |
| FR-CHAIN-010 | I | **PARTIAL** — `namir-engine`: FR-CHAIN-010 — the Verify: I method's "measure stage interaction against a specified probe signal" is unexecuted: the tagged test fills every channel with 0.0 and asserts 0.0 out with nothing loaded, which cannot distinguish any ordering of any stages from an empty chain; closes M8 |
| FR-CHAIN-020 | U | **PARTIAL** — `namir-engine`: FR-CHAIN-020 — of the four stages the "U per stage" method names, the NAM and IR bypass toggles are never exercised mid-signal (nam.rs:1080 and ir.rs:1151 both apply ENABLED=0 before any processing, then assert steady-state passthrough), and no test toggles one stage's bypass inside an assembled chain to show the others undisturbed; closes M8 |
| FR-CHAIN-030 | U | `namir-engine` |
| FR-CHAIN-040 | U | `namir-engine` |
| FR-CHAIN-050 | I | **PARTIAL** — `namir-engine`: FR-CHAIN-050 — the mono-core-then-duplicate behaviour is shown for GateStage only: NamStage has no multi-channel content assertion (every nam.rs test is ChannelConfig::Mono bar one that asserts nothing about channel content), and ChannelConfig::MonoToStereo appears in no gate.rs or nam.rs test at all; closes M8 |
| FR-CHAIN-060 | I | **PARTIAL** — `namir-engine`: FR-CHAIN-060 — of the table's three rows the "I per configuration" method names, the Mono→stereo row's IR-stage cell ("stereo IR, or dual mono IR") is exercised by nothing: no test loads any IR into an IrStage prepared with ChannelConfig::MonoToStereo; closes M8 |
| FR-CHAIN-080 | U | **PARTIAL** — `namir-engine`: FR-CHAIN-080 — the method's "inject a NaN into each stage's state" is performed on none of the six product stages: the only NaN write in the engine is chain.rs:546's test-local NanOnce fake writing into an output buffer, never into a real stage's filter, ramp, convolver or model state, and never through build_default_chain; closes M8 |
| FR-CHAIN-090 | U | `namir-engine` |
| FR-CLAP-010 | I | `namir-clap` |
| FR-CLAP-020 | S | `ci` |
| FR-CLAP-030 | I | **PARTIAL** — `namir-clap`: FR-CLAP-030 — the negotiation limb spans one configuration of the three FR-CHAIN-060 names. crates/namir-clap/src/audio_ports_ext.rs declares Stereo alone and implements no audio-ports-config extension, so Mono and Mono→stereo are never offered and no host can request them: there is no vtable entry for a test to call, in this harness or in any other host. Accepted 1.0 scope reduction per FR-CLAP-030's Consequence note (FRS, 2026-08-12), adjudicated at the 1.0 exit gate; closes M8 |
| FR-CLAP-040 | I | `namir-clap` |
| FR-CLAP-050 | I | **PARTIAL** — `namir-clap`: FR-CLAP-050 — the save direction of the host-driven half is unspanned: PluginStateImpl::save is called by no test, so nothing drives the clap_ostream adapter or the write-back through last_document it performs. load is driven through the real vtable by the host-ext-tests suites (clap_host_latency, clap_host_block_sizes, clap_host_rt_blocking, fr_cfg_020_shell_parity), which reach set_last_document and spawn_recall; two of its sequel steps still are not exercised — push_notice never runs, no test loading a document State::read warns on, and notify_params_changed's rescan is asserted by nothing, since TestHostMainThread::param_rescans has no reader; closes M8 |
| FR-CLAP-060 | I | **PARTIAL** — `namir-clap`: FR-CLAP-060 — both "sample-accurate" and "click-free" are unspanned, the tagged test asserting only that the bypass descriptor carries the IS_BYPASS flag and processing no audio; sample-accuracy is contradicted by apply_automation, which is called once before the block and applies every ParamValue event immediately without reading the event header's sample offset; closes M8 |
| FR-CLAP-070 | U | `namir-clap` |
| FR-CLAP-080 | I | **PARTIAL** — `namir-clap`: FR-CLAP-080 — swept at 154 rates (both endpoints, the six standard rates, a 1 kHz grid and one fractional value), not every rate the requirement's range admits: `src/audio.rs:125` rounds the host's rate to an integer, so the set a host can present collapses onto ~147 900 distinct `SampleRate` values, of which this file reaches 154. And nothing is loaded at any of them — this file never touches `PluginState`, so no model and no IR are present and D-9.2's `SlotResampler`, the one rate-dependent subsystem in the chain and the one this milestone found broken at 192 kHz, never runs at any rate here, the mid-session limb included, which asserts only a 1 kHz tone's RMS within 0.05 dB of the 48 kHz reading through a pass-through chain; closes M8 |
| FR-CLAP-090 | I | **PARTIAL** — `namir-clap`: FR-CLAP-090 — the B half of "I plus B", that N instances of one model use materially less memory than N separate copies, is measured by nothing: this crate's one bench (benches/plugin_instantiation.rs) times NFR-PERF-040's instantiation window, and no benchmark anywhere in the workspace measures memory at all; closes M8 |
| FR-CLAP-100 | I | **PARTIAL** — `namir-clap`: FR-CLAP-100 — the embedded-editor clause on macOS and Linux, where `is_api_supported` accepts only `GuiApiType::WIN32` with no `cfg` (issue #18), and `set_parent`, whose real embedding needs a live host window and stays in docs/manual-tests/fr-clap-100-gui-embedding.md; closes M8 |
| FR-CLAP-130 | S | **PARTIAL** — `namir-clap`: FR-CLAP-130 — the S half of "S plus I" is absent: nothing statically asserts that the audio-thread path takes no lock, makes no file-system call and performs no host round-trip, so only the I half is executed here; and of the three enumerated user actions only model loading and preset recall are driven for real, library scanning standing in as a worker job that reads, hashes and parses a multi-megabyte file while holding the same instance mutex a scan's jobs take, because starting a real scan would erase the developer's own library index; closes M8 |
| FR-EQ-010 | U | **PARTIAL** — `namir-engine`: FR-EQ-010 — the method's "magnitude response against the analytic target within 0.1 dB" is executed at no band's own frequency: every EQ-stage test measures gain at DC and at Nyquist only, against hand-written constants at 0.3 dB, so the low-shelf corner (40-500 Hz), the mid peak (200 Hz-5 kHz), the high-shelf corner (1-12 kHz) and the 0.2-5.0 Q range's effect on magnitude are unmeasured; closes M8 |
| FR-EQ-020 | U | **PARTIAL** — `namir-dsp`: FR-EQ-020 — two of the six sample rates the method names by number, 88.2 kHz and 176.4 kHz, are absent from the sweep's rate array, which is [44_100, 48_000, 96_000, 192_000]; closes M8 |
| FR-EQ-030 | U | **PARTIAL** — `namir-engine`: FR-EQ-030 — "changing any EQ parameter" spans one of EqStage's twelve: only ENABLED is toggled, and no test changes a band gain, a band frequency, mid Q, or either high-pass/low-pass defeat or corner frequency and measures click-freedom; closes M8 |
| FR-ERR-010 | I | **PARTIAL** — `namir-platform`: FR-ERR-010 — the configurable-verbosity and cannot-grow-without-bound clauses are spanned by the six clause helpers above; the per-user-location clause is asserted by nothing. Every clause drives a `Logger` built on a caller-supplied temporary path, and this test asserts `logging::logger().is_none()`, so `logging::init` — the only code binding the sink to a per-user path, via `crate::log_file_path` (`src/logging.rs:614-620`) — is called by no test. The nearest evidence, `src/paths.rs:123-133`, is `#[cfg(target_os)]`-gated, is vacuous when its environment variable is unset (`if let Some(...)`), and carries no tag for this requirement; closes M8 |
| FR-ERR-020 | S | **PARTIAL** — `namir-core`: FR-ERR-020 — the method's second conjunct, "every error path in the code maps to an entry", has no artifact of any kind: no xtask subcommand reads error paths, ErrorCode's three fields are all pub so a call site can construct an off-catalogue code inline, and crates/namir-ui/examples/manual_window_smoke.rs:27 is a live instance of exactly that; closes M8 |
| FR-ERR-030 | S | **PARTIAL** — `xtask`: FR-ERR-030 — the S half's logging limb only. The allocation limb is D-7.5's assert_no_alloc harness rather than this check; the "diagnostics ... communicated to a non-real-time thread without blocking" clause is spanned by nothing; and the `plus I` half of the Verify line has no integration test driving a real audio callback and asserting no record was emitted; closes M8 |
| FR-ERR-040 | I | **PARTIAL** — `namir-worker`: FR-ERR-040 — the method's "inject a fault into each non-audio subsystem" reaches only the generic worker thread pool: no fault is injected into the library scanner, the state save/load path, settings I/O, the resource cache or the GUI thread, and the plugin-configuration clause "continue passing audio, degraded if necessary" is asserted by nothing, the tagged test running no audio at all; closes M8 |
| FR-ERR-060 | S | **PARTIAL** — `workspace`: FR-ERR-060 — the method's "build-time check that no network-capable dependency is linked" is a by-name deny list its own comment calls deliberately non-exhaustive, and nothing detects direct std::net use in a first-party crate, which would open an outbound connection with Cargo.lock untouched and the gate green; closes M8 |
| FR-ERR-070 | S | `workspace` |
| FR-GATE-010 | U | **PARTIAL** — `namir-engine`: FR-GATE-010 — of the five controls the "U per control" method names, Attack and Release are exercised by no test: ATTACK_MS_ID is never written anywhere in the workspace, and RELEASE_MS_ID only as a helper inside a test whose assertion is about hold; closes M8 |
| FR-GATE-020 | U | `namir-dsp` |
| FR-GATE-030 | U | **PARTIAL** — `namir-dsp`: FR-GATE-030 — the requirement's "sample-accurate within the block, not stepped at block boundaries" distinction cannot be falsified by the tagged test, which drives one sample per process call so a block boundary and a sample boundary are the same event; the closing ramp's per-sample delta is never measured; closes M8 |
| FR-IN-010 | U | `namir-engine` |
| FR-IN-020 | U | **PARTIAL** — `namir-dsp`: FR-IN-020 — the "M for the display" half of the Verify line has no artifact: there is no docs/manual-tests/fr-in-020-*.md, and namir_ui::MeterReading carries only peak_db and rms_db, so the peak-hold value TrimStage publishes reaches no UI field for any script to observe; closes M8 |
| FR-IN-030 | U | **PARTIAL** — `namir-engine`: FR-IN-030 — the "resettable by the user" clause is unbuilt as well as unverified: Meter::reset_clip has no caller outside its own unit test, TrimStage exposes no clip-reset parameter and its Stage::reset is the transport-stop path, and UiIntent carries no reset-clip variant; closes M8 |
| FR-IO-010 | M | `docs/manual-tests/fr-io-010-device-enumeration.md` |
| FR-IO-020 | M | `docs/manual-tests/fr-io-020-wasapi-exclusive-mode.md` |
| FR-IO-030 | M | **UNRESOLVED** — `docs/manual-tests/fr-io-030-alsa-coreaudio.md` records `NOT EXECUTED this session (no Linux/macOS hardware available).` |
| FR-IO-040 | M | `docs/manual-tests/fr-io-010-device-enumeration.md` |
| FR-IO-050 | M | **UNRESOLVED** — `docs/manual-tests/fr-io-050-latency-measurement.md` records `PARTIAL.` |
| FR-IO-060 | I | **PARTIAL** — `namir-app`: FR-IO-060 — the "resettable by the user" clause has no path to exercise: XrunCounter::reset has no caller outside its own two unit tests and no UiIntent reaches it, and the running count surfaces only through an eprintln! rather than anywhere in the window; closes M8 |
| FR-IO-070 | I | **PARTIAL** — `namir-app`: FR-IO-070 — the method's named apparatus, a virtual device that can be made to fail on demand, does not exist and the tagged test opens no device, its whole body asserting that selecting from an empty slice is None, so device removal while in use, "stop the stream cleanly" and "allow the user to select another device" are all unexercised; closes M8 |
| FR-IO-080 | I | `namir-app` |
| FR-IR-010 | U | `namir-ir` |
| FR-IR-030 | U | **PARTIAL** — `namir-ir`: FR-IR-030 — measured across nine source/engine rate pairs drawn from the standard rates (44.1/48/88.2/96/192 kHz sources against 44.1/48/96 kHz engine rates), not every pair a `.wav` header and `SampleRate::new` between them admit: both take any nonzero rate, and where the lower of the two falls below about 40 kHz the imported FR-NAM-060 clause "up to 20 kHz or the Nyquist frequency, whichever is lower" is unsatisfiable by construction, an antialiasing filter flat to Nyquist not being able to be 100 dB down just above it, so that region needs an FRS decision rather than a test; closes M8 |
| FR-IR-040 | U | `namir-ir` |
| FR-IR-050 | U | `namir-ir` |
| FR-IR-060 | I | **PARTIAL** — `namir-engine`: FR-IR-060 — the no-dropout half of the FR-NAM-070 method this requirement imports is absent: the test asserts the discontinuity bound and the retire ring only, and a changeover that faded both slots to silence and back would lower that bound and pass; rt_stress.rs's dropout assertion never has an IR handover in flight, submitting Target::Nam alone; closes M8 |
| FR-IR-070 | U | **PARTIAL** — `namir-engine`: FR-IR-070 — of the four controls the "U per control" method names, the low cut's 20-500 Hz and high cut's 1 kHz-20 kHz ranges are set by no test: LOW_CUT_FREQ_HZ_ID and HIGH_CUT_FREQ_HZ_ID appear only in their declarations and live apply arms, and both cut tests run at the descriptor defaults probing DC and Nyquist, which is blind to the corner frequency; closes M8 |
| FR-IR-100 | U | `namir-engine` |
| FR-LIB-010 | I | **PARTIAL** — `namir-library`: FR-LIB-010 — the "the user shall be able to nominate one or more directories as library roots" clause has no mechanism to exercise: both shells open through LibraryService::open_at/open_default, which hard-code the single root <config_dir>/Library, AppSettings has no roots field and UiIntent has no add-root or remove-root variant; closes M8 |
| FR-LIB-020 | I | `namir-worker` |
| FR-LIB-030 | B | **PARTIAL** — `namir-library`: FR-LIB-030 — the Verify: B condition this requirement states, that a second start-up with an unchanged 10 000-file library is measurably faster than the first, is printed as CONCLUSIVE or INCONCLUSIVE and asserted by nothing; the persistence half is unmeasured too, arms C and D reusing an in-memory prior index rather than one written to disk and reloaded; closes M8 |
| FR-LIB-040 | U | `namir-library` |
| FR-LIB-070 | I | `namir-library` |
| FR-NAM-010 | U | `namir-nam` |
| FR-NAM-020 | G | `namir-nam` |
| FR-NAM-030 | G | **PARTIAL** — `namir-nam`: FR-NAM-030 — "each supported architecture" spans three configurations this crate runs, and the golden set holds two: WaveNet-A1 (this test) and LSTM (below). A2 has no golden model and no reference render under tests/golden/, and the A1 fixture cannot stand in for one -- it takes the A1 side of every A2 branch in wavenet.rs (per-layer kernel_sizes :1124, bottleneck distinct from channels :1138, the nested head :1152, the per-layer activation array :1167, the k-tap causal head conv :683-712), so the six parameterized Activation variants A2 introduced are all unreachable by it, LeakyReLU among them, which is what real A2 uses; closes M14; `namir-nam`: FR-NAM-030 — this half of the pair covers the LSTM architecture only. The third configuration the requirement quantifies over, WaveNet-A2, has no golden model and no reference render under tests/golden/ at all -- see the WaveNet test's own uncovered field above for why the A1 golden does not reach A2's code paths, and roadmap §21 Phase 4b for the fixture that closes both; closes M14 |
| FR-NAM-040 | U | `namir-nam` |
| FR-NAM-050 | I | **PARTIAL** — `namir-engine`: FR-NAM-050 — the comparison the Verify method specifies, a 48 kHz model driven at 44.1 kHz against the same model driven at 48 kHz with the input and output resampled offline to the FR-NAM-030 tolerance, is computed nowhere; the tagged test asserts only finiteness and non-allocation over 200 blocks of varying size; closes M8 |
| FR-NAM-060 | U | **PARTIAL** — `namir-engine`: FR-NAM-060 — measured across ten engine/model pairs drawn from the standard rates (44.1/48/88.2/96/176.4/192 kHz engine against 44.1/48/96 kHz models), not every pair `SampleRate::new` admits: it takes any nonzero u32, and where the lower of the two rates falls below about 40 kHz the requirement's own "or the Nyquist frequency, whichever is lower" clause is unsatisfiable by construction, an antialiasing filter flat to Nyquist not being able to be 100 dB down just above it, so that region needs an FRS decision rather than a test; closes M8 |
| FR-NAM-070 | I | `namir-engine` |
| FR-NAM-080 | U | **PARTIAL** — `namir-nam`: FR-NAM-080 — the "and display" half spans only the name field: UiSnapshot carries no metadata field beyond loaded_model_name, which namir-app fills from the file's basename rather than metadata.name, so author, gear make/model/type, tone type and description reach no screen; closes M8 |
| FR-NAM-090 | U | **PARTIAL** — `namir-engine`: FR-NAM-090 — the Verify: U method names "integrated loudness," ITU-R BS.1770's specific K-weighted, gated measurement; this test substitutes plain RMS-in-dB (rms_db's own doc comment states the substitution). The two agree for this test's own otherwise-matched signal pairs, which is why the behavior under test is real, but a true LUFS meter does not exist anywhere in this codebase, so the method is not executed as stated in the general case; closes M8 |
| FR-NAM-110 | U | **PARTIAL** — `namir-nam`: FR-NAM-110 — the method's "cross-correlate an impulse through the stage" is performed by nothing: both tagged tests read an accessor whose body is the literal 0 and assert it equals 0, so they would pass unchanged if inference did introduce delay, and the one path that reports a nonzero figure — NamStage's resampler latency — is asserted only as > 0 and is documented as not sample-exact; closes M8 |
| FR-NAM-130 | U | `namir-engine` |
| FR-NAM-140 | U | `namir-nam` |
| FR-NAM-150 | U | **PARTIAL** — `namir-nam`: FR-NAM-150 — the "to the accuracy of FR-NAM-030" clause. Both named configurations are spanned and the in-house reference is what this requirement's own Verify line elects, so neither is a gap, but the probe is deterministic_signal(99, 4_000): ~83 ms of 110 Hz sine plus low noise, not FR-NAM-030's specified 10-second clean/transient/saturated signal, and shorter than the real A2 shapes' ~6 346-sample receptive field, so every compared sample still depends on zero-padded startup history and the asserted figure never leaves the startup transient; closes M14 |
| FR-OUT-010 | U | **PARTIAL** — `namir-engine`: FR-OUT-010 — of the requirement's three literal parameters only exact silence at or below -60 dB is asserted; the +12 dB maximum and the 0 dB default are declared in namir_params::stages::out::GAIN_DB and read back by no test, params.lock recording only key, id, kind and smoothing; closes M8 |
| FR-OUT-020 | U | **PARTIAL** — `namir-engine`: FR-OUT-020 — of the four characteristics this requirement imports from FR-IN-020 and FR-IN-030, only the clip latch is asserted at the stage: no test reads OutStage's published peak_db, average_db or peak_hold_db telemetry entries, so a wiring error emitting one under another's id would pass, and the clip indicator is reachable by no user reset path; closes M8 |
| FR-PARAM-010 | U | **PARTIAL** — `namir-params`: FR-PARAM-010 — the method's "enumerate parameters and assert the completeness of each descriptor" is executed against no shipped parameter: the tagged test makes two format_value calls against a fabricated const declared inside mod tests, and nothing anywhere asserts a name, unit, minimum, maximum or default over REGISTRY's continuous entries, params.lock recording only key, id, kind and smoothing; closes M8 |
| FR-PARAM-020 | S | `xtask` |
| FR-PARAM-030 | I | **PARTIAL** — `namir-engine`: FR-PARAM-030 — of the three sources the requirement names, only two engine-internal delivery paths are compared: no artifact loads a namir_state::State and asserts the resulting engine state equals the state the same values reach via a parameter change, and no artifact drives namir_ui::UiIntent::SetParam into the comparison; closes M8 |
| FR-PARAM-040 | U | **PARTIAL** — `namir-dsp`: FR-PARAM-040 — the method's second half, "measure the artefact's spectral energy", is executed nowhere, and the frequency-affecting-parameter clause is unspanned: the EQ's frequencies and Q interpolate through Biquad::set_coeffs rather than GainRamp, and the only coefficient-ramp test asserts a relative comparison (ramped < jumped) rather than the 20 ms-linear-ramp bound this requirement names; closes M8 |
| FR-PARAM-050 | U | **PARTIAL** — `namir-params`: FR-PARAM-050 — none of the ten stepped parameters params.lock records is spanned: the tagged test asserts format_value against a fabricated const declared inside mod tests whose key appears in no registry, and nothing asserts that any shipped discrete parameter is Stepped rather than Continuous or that its values are named; closes M8 |
| FR-PKG-010 | S | `xtask` |
| FR-PKG-020 | S | `xtask` |
| FR-PKG-030 | M | `docs/manual-tests/fr-pkg-030-windows-install-scope.md` |
| FR-PKG-040 | S | **PARTIAL** — `xtask`: FR-PKG-040 — the requirement quantifies over "every distribution, installer and archive alike", and only macOS re-opens what it produced: make_installer.sh's verify_outputs unpacks the produced .pkg, mounts the .dmg and extracts the .zip, asserting the three files inside each (packaging/macos/make_installer.sh:717-769). Windows and Linux assert the *staging tree* only — namir.iss reads it, the ZIP and the .tar.gz are copies of it, and nothing opens the produced installer, ZIP or tarball. This test spans neither, asserting a synthetic staging tree, and ci.yml never runs xtask bundle at all, so every assertion against a real tree happens only on a v* tag in release.yml; closes M8 |
| FR-STATE-010 | U | `namir-state` |
| FR-STATE-020 | U | `namir-state` |
| FR-STATE-030 | I | **PARTIAL** — `namir-worker`: FR-STATE-030 — the save clause and both directions of "interchangeable between the standalone application and the CLAP plugin" are unspanned: the tagged test recalls an in-memory State and never writes or names a preset, and no artifact loads an app-written .namirpreset into the plugin or a plugin-written blob into the app; closes M8 |
| FR-STATE-040 | M | `docs/manual-tests/fr-state-040-diffability-and-hand-editability.md` |
| FR-STATE-050 | I | **PARTIAL** — `namir-worker`: FR-STATE-050 — the FR-NAM-070 constraints this requirement imports are unasserted through a recall: the tagged test asserts only that the model and IR changeovers never overlap, and processes no audio at all, so no-discontinuity and no-dropout under a continuous signal across a preset-driven changeover are checked by nothing; closes M8 |
| FR-STATE-060 | I | **PARTIAL** — `namir-worker`: FR-STATE-060 — the method's "save a project, restart the host, reopen" is executed by nothing: the artifact re-invokes its own test binary twice and drives namir_worker::Instance directly, never loading namir-clap or calling its state extension, and it writes both file references with an embedded blob and no path against an empty resolver, so the requirement's "identity of the loaded model and IR files" clause is bypassed; closes M8 |
| FR-STATE-070 | I | **PARTIAL** — `namir-state`: FR-STATE-070 — the third member of the failure list, "with an option to locate it manually", is spanned by nothing and exists nowhere in the product: UiIntent carries no locate or browse variant and neither shell offers such a path, the only mention in the tree being a doc comment paraphrasing the requirement; closes M8 |
| FR-UI-010 | S | `namir-ui` |
| FR-UI-020 | M | **UNRESOLVED** — `docs/manual-tests/fr-ui-020-single-screen-elements.md` records `NOT EXECUTED.` |
| FR-UI-030 | M | **UNRESOLVED** — `docs/manual-tests/fr-ui-030-accessibility-script.md` records `NOT EXECUTED this session — script above is ready to run by a person with a display,` |
| FR-UI-040 | M | **UNRESOLVED** — `docs/manual-tests/fr-ui-040-numeric-value-entry.md` records `NOT EXECUTED this session — script above is ready to run by a person with a display and` |
| FR-UI-050 | M | **UNRESOLVED** — `docs/manual-tests/fr-ui-050-reset-and-fine-adjust.md` records `NOT EXECUTED this session — script above is ready to run by a person with a display,` |
| FR-UI-060 | B | **PARTIAL** — `namir-ui`: FR-UI-060 — the requirement's stated condition, "while a library scan of 10 000 files is in progress", is absent from the measurement: the tagged test builds its snapshot with scan: None so render's scan branch never executes, and holds the index identity constant so ensure_filtered's re-filter path — the reason the memoization exists — never runs inside the timed frame; closes M8 |
| FR-UI-070 | M | **UNRESOLVED** — `docs/manual-tests/fr-ui-070-non-modal-error-notices.md` records `NOT EXECUTED.` |
| NFR-BUILD-010 | S | `workspace` |
| NFR-BUILD-020 | S | **PARTIAL** — `ci`: NFR-BUILD-020 — M12 closed the "document" half: README.md carries the build, run and test commands, and xtask identity asserts those exact strings are present, so they cannot silently *disappear* from it. They can still drift from this file — identity compares README.md against literals in xtask/src/identity.rs and nothing compares either against ci.yml, and they are already apart: this job runs `cargo build --workspace --all-targets` where the README documents `cargo build --workspace`. "Exercised by CI" is unmet on top of that — no step executes a command *because* it is documented (the test command matches by coincidence, the build command does not), and docs/user-guide.md's per-platform install and run instructions are unexercised. "Every product configuration on every supported platform" is unspanned too: no job in this file assembles a macOS bundle — release.yml's macos lane does, via xtask bundle and packaging/macos/make_installer.sh, but only on a v* tag, never on a push or pull request — and no job installs either product at its documented install path or loads the plugin in a real host, clap-validator and M9b's in-process host-ext-tests being neither an install nor a DAW; closes M8 |
| NFR-DOC-010 | M | `docs/manual-tests/nfr-doc-010-third-party-reader.md` |
| NFR-DOC-020 | S | `workspace` |
| NFR-DOC-040 | S | **PARTIAL** — `xtask`: NFR-DOC-040 — the "stating what it does" clause has no artifact: this check asserts that `# Namir`, the two licence file names and the three build/run/test command lines are present as substrings, and no static check can extend that to whether the prose around them actually describes the product; closes M8 |
| NFR-LIC-010 | S | **PARTIAL** — `workspace`, `xtask`: NFR-LIC-010 — the method's SPDX-header check has no artifact: only two tracked files under crates/ carry a header, and making the check meaningful means putting one on every file, which is NFR-LIC-060's (a Should's) work — accepted on that basis by the requirement's own Consequence note (FRS, 2026-08-12); closes M8 |
| NFR-LIC-020 | S | `workspace` |
| NFR-LIC-030 | S | **PARTIAL** — `xtask`: NFR-LIC-030 — the "shipped with the binaries" clause is asserted for one platform of three and only on a tag: M13's release.yml runs `xtask bundle`, which stages THIRD-PARTY-NOTICES.md beside the built artifacts and asserts it is there, and packaging/macos/make_installer.sh re-checks it inside the produced .pkg/.dmg/.zip — but nothing opens the Windows installer or ZIP or the Linux tarball to assert it, and ci.yml invokes `xtask bundle` nowhere, so no ordinary run checks any of it; closes M8 |
| NFR-LIC-040 | S | `workspace` |
| NFR-LIC-050 | S | **PARTIAL** — `namir-fixtures`: NFR-LIC-050 — the manifest the method names does not exist, and the traced artifact is a runtime generator whose output is never committed: the fifteen assets tracked under crates/*/fuzz/corpus, including a .wav impulse response and a .nam model, are recorded nowhere and no check would catch a captured asset added beside them; closes M8 |
| NFR-LIC-070 | S | `xtask` |
| NFR-PERF-010 | B | **PARTIAL** — `namir-engine`: NFR-PERF-010 — the "as a CI regression gate" half of Verify: B. M9b made the threshold an in-process assertion twice over (the contamination-immune estimator unconditionally, raw p99.9 over every repetition D-2.4 permits quoting), but the budget is 25% of one core of the 02-architecture.md section 2 reference machine and CI runs on GitHub-hosted shared runners, so ci.yml's nfr-perf-010-chain-bench job sets NAMIR_PERF_010_INFORMATIONAL and asserts nothing: no automated runner anywhere in this project gates on this number, which is what the requirement's method asks for and would need a self-hosted runner on that machine; closes M8 |
| NFR-PERF-020 | U | **PARTIAL** — `namir-engine`: NFR-PERF-020 — the first conjunct, that the engine adds no latency beyond what it reports, is measured nowhere: no test compares a chain's actual group delay against latency_samples() for any configuration where that value is nonzero, and the tagged test asserts 0 with nothing loaded, where every stage's own latency_samples() is 0 by construction; closes M8 |
| NFR-PERF-030 | B | **PARTIAL** — `namir-app`: NFR-PERF-030 — "audible" is taken to be RunningStreams::play() returning Ok(()), the instant both streams are told to run: no output callback is observed to have processed a block, so a launch that started its streams and then produced no sound would still be timed as having reached an audible state, and nothing automated anywhere confirms audio left the interface. The stronger marking event was considered and rejected: it needs an observable inside crate::stream's audio callback, added purely to enable a measurement; closes M8 |
| NFR-PERF-040 | B | `namir-clap` |
| NFR-PERF-050 | B | **PARTIAL** — `namir-worker`: NFR-PERF-050 — the "shall never delay the audio thread regardless of duration" clause: nothing in this binary runs an audio thread, so no arm here measures it. Its only evidence is rt_stress.rs's axis A, an integration test rather than the Verify: B this requirement names, whose concurrent loads are Nano fixtures and so exercise no long duration. The sentence's other clause, "within 500 ms for files up to 50 MB", closed at M9b: the file arms below time LoadSource::File, so fs::metadata and fs::read are inside the asserted window; closes M8 |
| NFR-PERF-060 | B | `namir-library` |
| NFR-PORT-010 | S | `ci` |
| NFR-PORT-020 | S | `xtask` |
| NFR-PORT-030 | S | `ci` |
| NFR-PORT-040 | S | `ci` |
| NFR-PORT-050 | U | `namir-state` |
| NFR-QUAL-010 | S | **PARTIAL** — `xtask`: NFR-QUAL-010 — the method's "fails on any uncovered Must" is not executed: CI's required step passes --allow-uncovered and derives its exit status from plan freshness and §14's denominators alone, while the plain form runs continue-on-error, so an uncovered Must would leave CI green — none stands today, and nothing gates on that; closes M9b |
| NFR-QUAL-020 | P | process (review + commit order, not build-inspectable) |
| NFR-QUAL-030 | G | `namir-ir`, `namir-nam` |
| NFR-QUAL-040 | S | **PARTIAL** — `namir-ir`, `namir-nam`, `namir-state`: NFR-QUAL-040 — of the parsers the method enumerates, the audio file reader is fuzzed header-only: the target calls probe_wav, which the crate documents as deliberately shallower, so decode's per-channel Vec::with_capacity, its MAX_LOAD_SECONDS clamp and PreparedIr::from_wav_bytes's resample and FFT planning — where a hang or an over-allocation would actually live — are never reached; closes M8 |
| NFR-QUAL-050 | S | `ci` |
| NFR-QUAL-060 | S | `ci` |
| NFR-RT-010 | S | **PARTIAL** — `namir-worker`: NFR-RT-010 — the allocation harness does not reach namir-app's audio callback: #[global_allocator] AllocDisabler is installed there (src/rt_harness.rs) but audio_section wraps only audio_io::convert's sample-format arithmetic, so the callbacks stream.rs drives through FakeBackend run under no assertion and the cpal data callbacks in audio_io.rs are executed by no test at all; closes M8 |
| NFR-RT-020 | S | `namir-engine` |
| NFR-RT-030 | B | **PARTIAL** — `namir-engine`: NFR-RT-030 — the method's "drive each stage with a denormal-producing signal" measures no stage individually: the binary compares the assembled six-stage chain's aggregate per-block p50 against nominal, in which a real spike confined to one stage is diluted below the asserted 10% bound by the other five; closes M8 |
| NFR-RT-040 | B | **PARTIAL** — `namir-engine`: NFR-RT-040 — the binary asserts no invariance and varies neither of the requirement's first two variables: all 105 000 blocks are driven with one xorshift noise material at one amplitude, and the gate and EQ parameters are set once before the measured loop and never varied within it, so "shall not depend on audio content or parameter values" is untested and the residue analysis is reported rather than asserted; closes M8 |
| NFR-SEC-010 | S | `namir-ir`, `namir-nam`, `namir-state` |
| NFR-SEC-020 | U | **PARTIAL** — `namir-ir`, `namir-library`, `namir-state`: NFR-SEC-020 — of the four file kinds namir-core's limits doc comment enumerates, the .nam/IR disk-load ceiling has no artifact: namir-worker/src/lib.rs:129 refuses a file over MAX_FILE_BYTES with FILE_TOO_LARGE and no test drives that branch, while namir-core's own limits test asserts only that the constant exceeds 50 MB, never that a file over it is refused; closes M8 |
| NFR-SEC-030 | S | `workspace` |

## Must requirements per FRS section

Derived from `docs/01-functional-requirements.md` per D-23.2: the section number comes from the FRS heading in force, the area token from the requirement ids. `docs/03-implementation-roadmap.md` §14's `### M9a re-audit` table must carry exactly these rows, in this order, with these denominators; `cargo run -p xtask -- traceability` fails when it does not. That table's three verdict columns are adjudicated by hand and are outside this check.

| FRS area | Must count |
|---|---|
| 4 CFG | 3 |
| 5.1 CHAIN | 8 |
| 5.2 IN | 3 |
| 5.3 GATE | 3 |
| 5.4 NAM | 13 |
| 5.5 IR | 7 |
| 5.6 EQ | 3 |
| 5.7 OUT | 2 |
| 5.8 PARAM | 5 |
| 5.9 STATE | 7 |
| 5.10 LIB | 5 |
| 5.11 IO | 8 |
| 5.12 CLAP | 11 |
| 5.13 UI | 7 |
| 5.14 ERR | 6 |
| 5.15 PKG | 4 |
| 6.1 RT | 4 |
| 6.2 PERF | 6 |
| 6.3 PORT | 5 |
| 6.4 QUAL | 6 |
| 6.5 LIC | 6 |
| 6.6 SEC | 3 |
| 6.7 BUILD | 2 |
| 6.8 DOC | 3 |
| **Total** | **130** |
